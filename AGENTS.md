# 영어 문제 제작 연구소 작업 규칙

## 작업 경계

- 이 저장소만 수정한다: `C:\Users\KMG\Desktop\영어 문제 제작 프로젝트(코덱스)`
- 원본 국어 저장소는 수정하지 않는다: `C:\Users\KMG\Desktop\문제 만들기 프로젝트(코덱스)`
- 관련 없는 기능과 사용자 데이터를 삭제하거나 초기화하지 않는다.
- 기존 JSON, localStorage, IndexedDB 데이터 호환성을 유지한다.

## 실행과 검증

- 로컬 영어 미리보기는 `http://127.0.0.1:5174/`을 사용한다.
- 기능 수정 후 `pnpm verify`를 실행한다.
- UI를 수정하면 실제 브라우저에서 동작을 확인하고 결과 보고 마지막에 미리보기 링크를 제공한다.
- Git commit, push, 저장소 연결은 사용자가 명시적으로 요청할 때만 한다.

## 구현 원칙

- 외부 AI API, 백엔드, 데이터베이스 서버를 임의로 추가하지 않는다.
- 수능형 프롬프트 규칙과 JSON 스키마를 변경할 때 `docs/english-gpt`의 지식 파일과 관련 테스트도 함께 확인한다.
- 시험지 조판 변경은 문제지와 정답·해설지, 인쇄와 PDF를 함께 검증한다.
- 큰 파일을 분리할 때는 먼저 동작 보존 테스트를 마련하고 작은 단계로 이동한다.
